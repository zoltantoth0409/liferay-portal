definition {
	macro configureDisplayCountPG {
		Portlet.gotoPortletOptions(
			portletName = "Announcements",
			portletName = "${portletName}",
			portletOption = "Configuration"
		);

		IFrame.selectDialogFrame();

		Select(locator1 = "Select#MAXIMUM_ITEMS_TO_DISPLAY", value1 = "5");

		PortletEntry.save();

		SelectFrameTop();
	}

	macro configurePaginationManageEntriesPG {
		Select(
			locator1 = "AnnouncementsManageEntries#DISTRIBUTION_SCOPE_SELECT",
			value1 = "${distributionScope}"
		);

		Click(locator1 = "Button#ITEMS_PER_PAGE");

		var key_itemsPerPage = "${itemsPerPage}";

		AssertClick(
			locator1 = "MenuItem#ANY_MENU_ITEM",
			value1 = "${itemsPerPage}",
			key_menuItem = "${itemsPerPage}"
		);
	}
}