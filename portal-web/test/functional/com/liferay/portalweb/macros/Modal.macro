definition {

	macro save {
		Button.clickSave();

		Alert.viewSuccessMessage();
	}

	macro selectFrameSpecific {
		SelectFrame(
			locator1 = "IFrame#MODAL_ANY"
			key_title = "${modalTitle}"
		);
	}
}