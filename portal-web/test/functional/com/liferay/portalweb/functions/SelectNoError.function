@default = "pauseSelect"
definition {
	function pauseSelect {
		WaitForSPARefresh();

		selenium.pause("5000");

		WaitForSPARefresh();

		selenium.waitForVisible();

		selenium.mouseOver();

		selenium.select();
	}
}