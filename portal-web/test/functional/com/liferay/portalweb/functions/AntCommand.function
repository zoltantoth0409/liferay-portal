@default = "antCommand"
@summary = "ant -f ${locator1} ${value1}"
definition {

	function antCommand {
		selenium.antCommand();

		selenium.assertJavaScriptErrors();

		selenium.assertLiferayErrors();
	}

}