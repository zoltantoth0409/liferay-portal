def instanceId = doc.instanceId.value;

if (doc.uid.value.startsWith('WorkflowMetricsSLAProcessResult') &&
	(doc.elapsedTime.value != 0) && !doc.onTime.empty) {

	state.map.merge(
		instanceId, doc.onTime.value,
		(onTime1, onTime2) -> (onTime1 && onTime2));
}