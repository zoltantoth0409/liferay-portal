task runGradleTest

gradle.liferayWorkspace {
	bundleUrl = uri("bundlesDir/bundle.zip")
}

runGradleTest {
	doLast {
		try {
			downloadBundle.execute()
		}
		catch (Exception e) {
			assert e.cause.message == "Destination cannot be the same as bundle url"
		}
	}
}