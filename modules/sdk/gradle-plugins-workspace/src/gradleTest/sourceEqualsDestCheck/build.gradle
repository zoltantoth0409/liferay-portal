task runGradleTest

gradle.liferayWorkspace {
	bundleCacheDir = "bundlesDir"
	bundleUrl = uri("bundlesDir/bundle.zip")
}

runGradleTest {
	doLast {
		try {
			downloadBundle.execute()
		}
		catch (Exception e) {
			assert e.cause.message.endsWith(" cannot be the same")
		}
	}
}