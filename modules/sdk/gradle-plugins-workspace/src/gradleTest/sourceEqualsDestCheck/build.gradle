task runGradleTest

gradle.liferayWorkspace {
	bundleUrl = uri("bundlesDir/bundles.zip")
}

runGradleTest {
	dependsOn downloadBundle

	doLast {
		assert downloadBundle.state.failure.contains("Destination Dir cannot be the same as Source Dir")
	}
}