dependencies {
	compile group: "com.liferay", name: "com.liferay.portal.target.platform.indexer", version: "4.0.7"
	compile group: "org.osgi", name: "osgi.core", version: "5.0.0"
}

distributions {
	main {
		contents {
			from(projectDir) {
				include "run.sh"
				into "lib"
			}
		}
	}
}