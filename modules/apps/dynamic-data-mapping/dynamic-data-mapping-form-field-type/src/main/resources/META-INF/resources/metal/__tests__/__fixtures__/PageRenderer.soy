{namespace PageRenderer}

{template .render}
	{@param items: list<[
		type: string
	]>}

	<div>
		{foreach $item in $items}
			{let $type: $item.type /}

			{delcall PageRenderer.RegisterFieldType variant="$type" /}
		{/foreach}
	</div>
{/template}