components:
    schemas:
        Experiment:
            properties:
                id:
                    format: int64
                    readOnly: true
                    type: integer
info:
    description: ""
    title: "Segments Asah"
    version: v1.0
openapi: 3.0.1
paths:
    "/experiments/{experimentId}":
        patch:
            description: ""
            operationId: patchExperiment
            parameters:
                - in: path
                  name: experimentId
                  required: true
                  schema:
                      format: int64
                      type: integer
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: "#/components/schemas/Experiment"
                    application/xml:
                        schema:
                            $ref: "#/components/schemas/Experiment"
            responses:
                200:
                    content:
                        application/json:
                            schema:
                                $ref: "#/components/schemas/Experiment"
                        application/xml:
                            schema:
                                $ref: "#/components/schemas/Experiment"
                    description: ""
            tags: ["Experiment"]