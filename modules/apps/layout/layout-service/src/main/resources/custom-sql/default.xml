<?xml version="1.0"?>

<custom-sql>
	<sql id="com.liferay.layout.service.persistence.LayoutClassedModelUsageFinder.countByC_C">
		<![CDATA[
			SELECT
				COUNT(DISTINCT LayoutClassedModelUsage.plid) AS COUNT_VALUE
			FROM
				LayoutClassedModelUsage
			WHERE
				(LayoutClassedModelUsage.classNameId = ?) AND
				(LayoutClassedModelUsage.classPK = ?)
		]]>
	</sql>
</custom-sql>