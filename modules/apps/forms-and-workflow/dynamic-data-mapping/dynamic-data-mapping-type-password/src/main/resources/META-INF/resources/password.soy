{namespace DDMPassword}

/**
 * Defines the delegated template for the password field.
 */
{deltemplate ddm.field variant="'password'"}
	{call .render data="all" /}
{/deltemplate}

/**
 * Prints the DDM form password field.
 */
{template .render}
	{@param? dir: string}
	{@param? label: string}
	{@param name: string}
	{@param placeholder: string}
	{@param readOnly: bool}
	{@param required: bool}
	{@param showLabel: bool}
	{@param? tip: string}
	{@param? tooltip: string}
	{@param value: string}
	{@param visible: bool}

	<div class="form-group{$visible ? '' : ' hide'} liferay-ddm-form-field-password {$tip ? 'liferay-ddm-form-field-has-tip' : ''}" data-fieldname="{$name}">
		{if $showLabel}
			<label class="control-label" for="{$name}">
				{$label}

				{if $required}
					<span class="icon-asterisk text-warning"></span>
				{/if}
			</label>

			<p class="liferay-ddm-form-field-tip">{$tip ? $tip: ''}</p>
		{/if}

		<div class="input-group-container {if $tooltip}input-group-default{/if}">
			<input class="field form-control" {if $dir}dir="{$dir}"{/if} id="{$name}" name="{$name}" placeholder="{$placeholder}" {if $readOnly}readonly{/if} type="password" value="{$value}">

			{if $tooltip}
				<span class="input-group-addon">
					<span class="input-group-addon-content">
						<a class="help-icon help-icon-default icon-monospaced icon-question" data-original-title="{$tooltip}" data-toggle="popover" href="javascript:;" title="{$tooltip}"></a>
					</span>
				</span>
			{/if}
		</div>
	</div>
{/template}