@component-name = "portal-wcm"
definition {

    property portal.release = "true";
	property portal.upstream = "true";
	property testray.main.component.name = "Web Content Administration";

    setUp {
		TestCase.setUpPortalInstance();

		User.firstLoginPG();

		JSONGroup.addGroup(groupName = "Test Site Name");
	}

    tearDown {
		var testPortalInstance = PropsUtil.get("test.portal.instance");

		if ("${testPortalInstance}" == "true") {
			PortalInstances.tearDownCP();
		}
		else {
			JSONGroup.deleteGroupByName(groupName = "Test Site Name");
		}
	}

    @description = "This is a test for LPS-98071. As a Developer, I want to Implement the geolocation field type"
	@ignore = "true"
	@priority = "5"
	test CreateGeolocationField {
		property portal.acceptance = "false";

		// TODO LPS-98071 CreateGeolocationField pending implementation

	}

    @description = "This is a test for LPS-98071. As a Developer, I want to Implement the geolocation field type"
	@ignore = "true"
	@priority = "5"
	test VerifySearchablePropertyOfGeolocation {
		property portal.acceptance = "false";

		// TODO LPS-98071 VerifySearchablePropertyOfGeolocation pending implementation

	}

    @description = "This is a test for LPS-98071. As a Developer, I want to Implement the geolocation field type"
	@ignore = "true"
	@priority = "3"
	test ShowAndHideGeolocationLabel {
		property portal.acceptance = "false";

		// TODO LPS-98071 ShowAndHideGeolocationLabel pending implementation

	}

    @description = "This is a test for LPS-98071. As a Developer, I want to Implement the geolocation field type"
	@ignore = "true"
	@priority = "4"
	test DuplicateGeolocationFieldOnWebContent {
		property portal.acceptance = "false";

		// TODO LPS-98071 DuplicateGeolocationFieldOnWebContent pending implementation

	}

}